import 'package:flutter_test/flutter_test.dart';
import 'package:gst_audio_flutter/main.dart';

void main() {
  test('app widget can be constructed', () {
    expect(const AudioPlayerApp(), isA<AudioPlayerApp>());
  });
}
